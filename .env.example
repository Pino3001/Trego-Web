# Opcional: base URL de la API (vacío = mismo origen + proxy Vite /api → localhost:8080 en desarrollo)
# VITE_API_URL=

# Geoapify: autocomplete y calle/número desde GPS en el carrito
# VITE_GEOAPIFY_KEY=tu_api_key
